import React from 'react'

// Define interface for stopwatch display props

export default function StopWatch() {
    return(
        <div>
            {/* Use props to display the time elapsed during the current lap and if laps exist, display lap times. */}
        </div>
    )
}